country_decisions = {

	oku_release_okuu_from_start = {
		potential = {	
			tag = CRD
			NOT = { exists = OKU }
			NOT = { has_country_flag = is_releasing_okuu }
			NOT = { is_year = 1445 }
		}
		allow = { NOT = { is_year = 1445 } }
		effect = {
			hidden_effect = {
				set_country_flag = is_releasing_okuu
			}
			country_event = { 
				id = flavor_oku.1
			}
		}
		major = yes
		
		ai_will_do = {
			factor = 0
		}
	}
}
