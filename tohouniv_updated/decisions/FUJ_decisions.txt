country_decisions = {
	
	fuj_respawn_mokou = {
		potential = {
			NOT = { has_ruler_flag = th_original_mokou }
			NOT = { has_country_modifier = forbid_respawn_cd }
			OR = {
				tag = FUJ
				was_tag = FUJ
			}
		}

		major = yes
		
		allow = {
			if = {
				limit = {
					NOT = { has_country_flag = th_free_respawn }
				}
				mil_power = 150
			}
			th_ruler_is_not_a_masked_illusion = yes
		}
			
		effect = {
			if = {
				limit = {
					NOT = { has_country_flag = th_free_respawn }
				}
				add_mil_power = -150
			}
			country_event = {
				id = flavor_fuj.101
			}
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	fuj_wear_the_fire_rat_robe = {
		potential = {
			has_country_flag = fuj_fire_rat_robe_available
		}
		
		allow = {
			always = yes
		}
			
		effect = {
			add_country_modifier = {
				name = fuj_fire_rat_robe_buff
				duration = 1825
			}
			hidden_effect = {
				clr_country_flag = fuj_fire_rat_robe_available
			}
		}
		
		ai_will_do = {
			factor = 100
		}
	}
}
