country_decisions = {
	
	fom_call_patchouli = {
		potential = {
			has_country_flag = fom_patchouli_joined
			OR = {
				tag = FOM
				was_tag = FOM
			}
		}
		allow = {
			4827 = {
				country_or_non_sovereign_subject_holds = ROOT
			}
			custom_trigger_tooltip = {
				tooltip = fom_patchouli_hired_for_thirty_years
				NOT = { advisor = th_advisor_patchouli }
				OR = {
					NOT = { has_country_flag = fom_patchouli_hired }
					had_country_flag = {
						flag = fom_patchouli_hired
						days = 10950
					}
				}
			}
		}	
		effect = {
			custom_tooltip = fom_hire_patchouli.tt
			hidden_effect = {	
				country_event = {
					id = flavor_fom.102
				}
			}
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	fom_respawn_alice = {
		potential = {
			NOT = { has_ruler_flag = touhou_original_heir }	#For the case Alice is the new ruler
			NOT = { has_heir_flag = touhou_original_heir }
			OR = {
				tag = FOM
				was_tag = FOM
			}
		}

		major = yes
		
		allow = {
			mil_power = 150
		}
			
		effect = {
			add_mil_power = -150
			country_event = { 	#Alice alone returns
				id = flavor_fuj.101
			}
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	#For the case Marisa reached immortality
	fom_respawn_marisa = {
		potential = {
			NOT = { has_ruler_flag = touhou_original_heir }	#For the case Alice is the new ruler
			NOT = { has_heir_flag = touhou_original_heir }
			OR = {
				tag = FOM
				was_tag = FOM
			}
		}

		major = yes
		
		allow = {
			mil_power = 150
		}
			
		effect = {
			add_mil_power = -150
			country_event = { 	#Alice alone returns
				id = flavor_fuj.101
			}
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	fom_fight_mai_yuki = {
		potential = {
			has_country_flag = fom_can_fight_mai_yuki
		}
		allow = {
			mil_power = 100
			custom_trigger_tooltip = {
				tooltip = fom_lost_against_mai_yuki_tt
				OR = {
					NOT = { has_country_flag = fom_lost_against_mai_yuki }
					had_country_flag = {
						flag = fom_lost_against_mai_yuki
						days = 1825
					}
				}
			}
			th_incident_is_not_investigator = yes
		}	
		major = yes
		effect = {
			custom_tooltip = fom_fight_mai_yuki.tt
			add_mil_power = -100
			hidden_effect = {	
				country_event = {
					id = flavor_fom.12
				}
				clr_country_flag = fom_lost_against_mai_yuki
			}
		}
		
		ai_will_do = {
			factor = 100
		}
	}
}