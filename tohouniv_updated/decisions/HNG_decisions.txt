country_decisions = {

	hng_release_meiling_from_start = {
		potential = {	
			tag = SCR
			NOT = { exists = HNG }
			NOT = { has_country_flag = is_releasing_meiling }
			NOT = { is_year = 1445 }
			always = no #Will be added in the next update
		}
		allow = { NOT = { is_year = 1445 } }
		effect = {
			hidden_effect = {
				set_country_flag = is_releasing_meiling
			}
			country_event = { 
				id = flavor_hng.1
			}
		}
		major = yes
		
		ai_will_do = {
			factor = 0
		}
	}
}
