cirnia_missions_independency_war_slot = {
	slot = 2
	generic = no
	ai = yes
	potential = {
		tag = CIN
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes
	
	cin_restore_our_sovereignty = {
		icon = mission_touhou_flan_royal_fighter
		required_missions = {  }
		position = 1
		
		trigger = {
			is_subject = no
			OR = {
				SCR = { war_with = ROOT }
				NOT = { exists = SCR }
			}
		}
		effect = {
			add_stability = 2
			change_government_reform_progress = 100
		}
	}
}

cirnia_missions_conquest_slot = {
	slot = 3
	generic = no
	ai = yes
	potential = {
		tag = CIN
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes
}

cirnia_missions_independency_supporters_slot = {
	slot = 4
	generic = no
	ai = yes
	potential = {
		tag = CIN
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes
	
	cin_get_used_to_europe = {
		icon = mission_touhou_noobie_flan_throne
		required_missions = {  }
		position = 1
		trigger = {
			custom_trigger_tooltip = {
				tooltip = th_cin_any_country_likes_us_tt
				any_country = {
					capital_scope = {
						superregion = gensokyo_superregion
					}
					NOT = { overlord_of = ROOT }
					is_free_or_tributary_trigger = yes
					has_opinion = {
						who = ROOT
						value = 125
					}
				}
			}
		}
		effect = {
			add_stability = 2
		}
	}
}