th_moriya_formable = {
	slot = 1
	generic = no
	ai = yes
	potential = {
		OR = {
			tag = KMS
			tag = MGT
			tag = NMN
		}
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes

	th_forest_war_in_forest = {
		icon = mission_mariali_fight
		required_missions = {  }
		position = 1
		trigger = {
			capital_scope = {
				culture = th_magician_culture
			}
			monthly_mil = 9
			OR = {
				employed_advisor = {
					category = MIL
				}
				num_of_generals = 1
			}
		}
		effect = {
			edge_of_the_forest_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			kirisame_forest_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			marisas_home_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			mushroom_grove_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			forest_coast_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			tsuchinoko_hunting_grounds_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			fairy_forest_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			matriold_woods_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			forest_of_magic_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			magic-rich_thickset_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			custom_tooltip = th_has_magician_supremacy_war_cb_flag_tt
			hidden_effect = {
				set_country_flag = th_has_magician_supremacy_war_cb_flag
			}
		}
	}

	th_forest_unite_of_forest = {
		icon = mission_touhou_forest_of_magic
		required_missions = { th_forest_war_in_forest }
		position = 2
		provinces_to_highlight = {
			OR = {
				area = edge_of_the_forest_state
				area = kirisame_forest_state
				area = marisas_home_state
				area = mushroom_grove_state
				area = forest_coast_state
				area = tsuchinoko_hunting_grounds_state
				area = fairy_forest_state
				area = matriold_woods_state
				area = forest_of_magic_state
				area = magic-rich_thickset_state
			}
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				or = {
					area = edge_of_the_forest_state
					area = kirisame_forest_state
					area = marisas_home_state
					area = mushroom_grove_state
					area = forest_coast_state
					area = tsuchinoko_hunting_grounds_state
					area = fairy_forest_state
					area = matriold_woods_state
					area = forest_of_magic_state
					area = magic-rich_thickset_state
				}
				value = 35
			}
			if = {
				limit = {
					tag = KMS
				}
				MGT = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
				NMN = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
			}
			if = {
				limit = {
					tag = MGT
				}
				KMS = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
				NMN = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
			}
			if = {
				limit = {
					tag = NMN
				}
				KMS = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
				MGT = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
			}
		}
		effect = {
			eastern_forest_region = {
				limit = {
					owned_by = ROOT
					NOT = { is_core = ROOT }
				}
				add_core = ROOT
				add_local_autonomy = -100
				add_nationalism = -100
			}
			fairy_forest_state = {
				limit = {
					owned_by = ROOT
					NOT = { is_core = ROOT }
				}
				add_core = ROOT
				add_local_autonomy = -100
				add_nationalism = -100
			}
		}
	}
}