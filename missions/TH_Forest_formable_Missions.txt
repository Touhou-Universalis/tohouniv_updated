th_moriya_formable = {
	slot = 1
	generic = no
	ai = yes
	potential = {
		OR = {
			tag = KMS
			tag = MGT
			tag = NMN
		}
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes

	th_forest_war_in_forest = {
		icon = mission_mariali_fight
		required_missions = {  }
		position = 1
		trigger = {
			capital_scope = {
				culture = th_magician_culture
			}
			monthly_mil = 9
			OR = {
				employed_advisor = {
					category = MIL
				}
				num_of_generals = 1
			}
		}
		effect = {
			forest_edge_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			kirisame_magic_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			marisa_place_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			mushroom_cluster_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			forest_coast_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			tsuchinoko_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			abandoned_fairy_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			alices_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			forest_of_magic_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			magic-rich_thickset_state = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			custom_tooltip = th_has_magician_supremacy_war_cb_flag_tt
			hidden_effect = {
				set_country_flag = th_has_magician_supremacy_war_cb_flag
			}
		}
	}

	th_forest_unite_of_forest = {
		icon = mission_touhou_forest_of_magic
		required_missions = { th_forest_war_in_forest }
		position = 2
		provinces_to_highlight = {
			OR = {
				area = forest_edge_state
				area = kirisame_magic_area
				area = marisa_place_state
				area = mushroom_cluster_state
				area = forest_coast_state
				area = tsuchinoko_state
				area = abandoned_fairy_state
				area = alices_area
				area = forest_of_magic_area
				area = magic-rich_thickset_state
			}
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				or = {
					area = forest_edge_state
					area = kirisame_magic_area
					area = marisa_place_state
					area = mushroom_cluster_state
					area = forest_coast_state
					area = tsuchinoko_state
					area = abandoned_fairy_state
					area = alices_area
					area = forest_of_magic_area
					area = magic-rich_thickset_state
				}
				value = 35
			}
			if = {
				limit = {
					tag = KMS
				}
				MGT = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
				NMN = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
			}
			if = {
				limit = {
					tag = MGT
				}
				KMS = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
				NMN = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
			}
			if = {
				limit = {
					tag = NMN
				}
				KMS = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
				MGT = {
					OR = {
						exists = no
						is_subject_of = ROOT
					}
				}
			}
		}
		effect = {
			eastern_forest_region = {
				limit = {
					owned_by = ROOT
					NOT = { is_core = ROOT }
				}
				add_core = ROOT
				add_local_autonomy = -100
				add_nationalism = -100
			}
			abandoned_fairy_state = {
				limit = {
					owned_by = ROOT
					NOT = { is_core = ROOT }
				}
				add_core = ROOT
				add_local_autonomy = -100
				add_nationalism = -100
			}
		}
	}
}