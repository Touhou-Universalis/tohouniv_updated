namespace = flavor_tsk

country_event = {
	id = flavor_tsk.1
	title = "flavor_tsk.1.t"
	desc = "flavor_tsk.1.desc"
	picture = TSUKASA_eventPicture
	
	fire_only_once = yes
	is_triggered_only = yes
	
	option = {		#No thanks
		name = flavor_tsk.1.a
		
		ai_chance = { 
			factor = 0
		}
	}

	option = {		#Let the player play as Tsukasa
		name = flavor_tsk.1.b
		highlight = yes

		
		change_tag = TSK
		add_government_reform = Gensokyan_monarchy
		add_prestige = 50
		if = {
			limit = {
				NOT = { has_idea_group = TSK_ideas }
				is_playing_custom_nation = no
			}
			country_event = {
				id = ideagroups.1
			}
		}
		ai_chance = { 
			factor = 100
		}
	}
}

