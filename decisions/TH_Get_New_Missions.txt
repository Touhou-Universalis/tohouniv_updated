#TODO 
country_decisions = {
	ymu_japanese_missions = {
		potential = {
			tag = YMU
			NOT = { has_country_flag = ymu_unlock_jap }	
			NOT = { has_country_flag = formed_japan_flag }
		}
		provinces_to_highlight = {
			OR = {
				province_id = 1020
				province_id = 1028
			}
			NOT = { owned_by = ROOT }
		}
		allow = { 
			NOT = { exists = JAP }
			is_free_or_tributary_trigger = yes
			owns = 1020
			owns = 1028
			is_at_war = no
			
			if = {
				limit = {
					any_country = {
						vassal_of = ROOT
						has_reform = daimyo
					}
				}
				OR = {
					all_country = {
						NOT = { has_reform = daimyo }
						NOT = { has_reform = indep_daimyo }
						NOT = { has_reform = shogunate }
					}
					num_of_owned_provinces_with = {
						value = 25
						region = japan_region
					}
				}
			}
			else = {
				all_country = {
					NOT = { has_reform = daimyo }
					NOT = { has_reform = indep_daimyo }
					NOT = { has_reform = shogunate }
				}
			}
		}
		effect = {
			set_country_flag = ymu_unlock_jap
			swap_non_generic_missions = yes  
			
			remove_government_reform = shogunate
			remove_government_reform = daimyo
			remove_government_reform = indep_daimyo

			set_country_flag = formed_japan_flag
			set_government_rank = 3

			if = {
				limit = {
					any_country = {
						vassal_of = ROOT
						has_reform = daimyo
					}
				}
				custom_tooltip = unite_japan_subject_tooltip
				hidden_effect = {
					every_subject_country = {
						limit = {
							has_reform = daimyo
						}
						ROOT = {
							free_vassal = PREV
						}
						remove_government_reform = daimyo
						every_owned_province = {
							remove_core = YMU
						}
					}
				}
			}

			add_legitimacy = 100
			add_adm_power = 100
			add_dip_power = 100
			add_mil_power = 100

			if = { 
				limit = { 
					japan_region = { NOT = { country_or_non_sovereign_subject_holds = ROOT } } 
				}
				japan_region = {
					limit = {
						NOT = { is_core = ROOT } 
						NOT = { owned_by = ROOT } 
					}
					add_permanent_claim = ROOT 
				}
			}
		}
		major = yes
		
		ai_will_do = {
			factor = 0
		}
	}

	pr1_persian_missions = {
		potential = {
			NOT = {has_country_flag = pr1_unlock_persia}	
			tag = PR1
		}
		allow = { 
			
		}
		effect = {
			set_country_flag = pr1_unlock_persia
			country_event = {
				id = flavor_prs.3
			}
		}
		major = yes
		
		ai_will_do = {
			factor = 0
		}
	}



}




