#TODO 
country_decisions = {
	ymu_respawn_ymu = {
		potential = {
			NOT = { has_ruler_flag = touhou_original_ruler}
			OR = {
				tag = YMU
				was_tag = YMU
			}
		}

		major = yes
		
		allow = {
			if = {
				limit = {
					NOT = { has_country_flag = th_free_respawn }
				}
				mil_power_cost = 100
			}
		}
			
		effect = {
			if = {
				limit = {
					NOT = { has_country_flag = th_free_respawn }
				}
				mil_power_cost = 100
			}
			country_event = {
				id = flavor_ymu.2
			}
		}
		
		ai_will_do = {
			factor = 0
		}
	}
}